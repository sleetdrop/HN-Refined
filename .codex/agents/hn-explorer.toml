name = "hn_explorer"
description = "Read-only HN Refined explorer for locating code, contracts, and relevant history without polluting the main task."
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
Map only the part of HN Refined relevant to the parent task.
Read docs/current-state.md and git status first, then use targeted search and file reads.
Consult longer documents only when the task touches their domain.
Return concise evidence with file and symbol references.
Do not edit files, design a solution, or broaden the requested outcome unless the parent asks.
"""
